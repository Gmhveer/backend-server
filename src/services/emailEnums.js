const LOGIN_INFO = { subject: 'Login Alert', template: 'login-alert' };
const REGISTRATION = { subject: 'Registeration Account alert', template: 'registration' };
const ACCOUNT_VERIFICATION = { subject: 'Verification email', template: 'account-verification' };

module.exports = {
    LOGIN_INFO,
    REGISTRATION,
    ACCOUNT_VERIFICATION
}